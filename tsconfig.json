// tsconfig.json
{
  "compilerOptions": {
    "target": "es2020",
    "lib": ["dom","dom.iterable","esnext"],
    "allowJs": false,
    "skipLibCheck": true,
    "strict": true,
    "forceConsistentCasingInFileNames": true,
    "noEmit": true,
    "module": "esnext",
    "moduleResolution": "node",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "esModuleInterop": true,

    // ← tell TS to look for your custom .d.ts files here
    "typeRoots": ["node_modules/@types", "src/types"]
  },

  // one single include; pulls in code + your types
  "include": [
    "next-env.d.ts",
    "src"
  ],

  "exclude": ["node_modules"]
}
